---
scope: TaskFlow: keep the 5-role wave-dispatch (Scrum Master, DB Engineer, Backend Dev, Frontend Dev, Integrator) subagents from diverging on shared contracts, per SPEC-taskflow CAP-1..8
purpose: build-substrate
altitude: feature
updated: 2026-07-19T22:39
---

- (decision) [ADOPTED] Stack pinned by CLAUDE.md, not re-derived: React 19 + TypeScript + Vite + Tailwind v4 + shadcn/ui (frontend), Node + Express + TypeScript (backend), SQLite + Drizzle ORM (data), JWT auth. Binds: all. Will still verify current versions/fit on the web before binding into Stack table.
- (decision) [ADOPTED] JWT issuance/verification library is jose, per epics-to-agents-handoff.md's worked example (Story 1.2: 'JWT issuance (jose)'). Binds: CAP-1, CAP-2.
- (decision) [ADOPTED] Every API response follows {data: T} success / {error: string} failure envelope, per CLAUDE.md Coding Standards. Prevents: Backend and Frontend inventing incompatible response shapes independently. Binds: all API endpoints, CAP-1..7.
- (decision) [ADOPTED] File ownership: DB Engineer -> src/db/, Backend Dev -> src/api/, Frontend Dev -> src/client/, Integrator -> tests/ + src/client/lib/api.ts + .env, Scrum Master -> _bmad_output. No agent writes outside its own directory. Prevents: merge conflicts between parallel subagent dispatches. Binds: CAP-8.
- (decision) [ADOPTED] Delivery is 5 sequential waves per story (Scrum Master -> DB -> Backend -> Frontend -> Integrator), each gated by bmad-code-review before its own PR opens and before the next wave dispatches. Binds: CAP-8.
- (decision) [ADOPTED] Same-directory parallel dispatches within one wave must consume shared seams rather than invent their own, per epics-to-agents-handoff.md: a shared useTaskBoard hook, a shared response-envelope helper, a token-handoff convention, a canonical Task DTO, and a shared test harness. These five named seams are exactly what this spine's ADs need to formalize (they were flagged as future AD-4/AD-8/AD-10/AD-11/AD-12 in the handoff doc, and AD-9 as synchronous db.transaction callbacks).
- (note) Reading SPEC-taskflow's memlog + CLAUDE.md + epics-to-agents-handoff.md as the input. No project-context.md exists (persistent_facts pattern found nothing) - greenfield, no existing code to ratify conventions from yet.
- (direction) User chose Coaching path and spine-only deliverable (no extra walkthrough artifact) - this is a solo learning project, no external audience.
- (decision by user) SQLite driver: better-sqlite3. Binds: src/db/, all Backend queries. Prevents: mixed sync/async DB access patterns across independently-dispatched Backend sub-units.
- (decision by user) Task reordering: gap-based integer order values (e.g. step 1000), new task's order set to midpoint of its neighbors on insert/move. Binds: CAP-3, CAP-5. Prevents: Backend and Frontend disagreeing on move/reindex semantics; avoids O(n) reindex storms.
- (decision by user) API layering: routes in src/api/routes/ call Drizzle directly, no service/repository layer. Binds: CAP-1..7, all Backend Dev sub-units. Prevents: parallel Backend dispatches inventing incompatible layering conventions; matches CLAUDE.md's existing src/api/ tree (no services/ folder shown).
- (version) Verified on web (2026-07-19): react/react-dom 19.2.7 (matches CLAUDE.md's React 19), vite 8.1.5, tailwindcss 4.3.2 (matches CLAUDE.md's Tailwind v4), express 5.2.1 (Express 5 is now the TC-endorsed active line, not 4 - recommend pinning to 5.x), drizzle-orm 0.45.2, better-sqlite3 12.11.1, jose 6.2.3, bcrypt 6.0.0. All current and fit; no named technology is stale or discontinued.
- (version) TypeScript: 7.0 shipped 2026-07-08 (Go-based rewrite, ~10x faster) but is 11 days old at authoring time; TypeScript 6.0 (2026-03-23) is the mature JS-compiler bridge release. Recommend pinning to TypeScript 6.x for this project to avoid brand-new-major toolchain risk (ts-node/loader compat); revisit once TS7 ecosystem support matures.
- (version) Node.js: 24 is current active LTS, 22 is still maintenance LTS. Dev machine already has Node v22.3.0 installed. Recommend engines >=22 to avoid forcing a Node upgrade for this learning project; no functional need for 24.
- (version) shadcn/ui: CLI v4 (March 2026) is current, registry-based (not a single pinned npm version) - use CLI v4 with --base radix (Vite template support is first-class in CLI v4).
- (event) Spine drafted: 12 ADs (AD-4/8/9/10/11/12 deliberately numbered to match the forward references already used in epics-to-agents-handoff.md's worked example). Running lint_spine.py next, then Reviewer Gate.
- (event) lint_spine.py: 5 low-severity placeholder findings, all false positives ({data}/{error} are literal response-envelope notation matching CLAUDE.md's {data: T}/{error: string} convention, not unfilled template tokens). No fix needed. Dispatching Reviewer Gate's two floor reviewers (version-verification, adversarial-divergence) as parallel subagents next.
- (event) Reviewer Gate, version-verification lens: PASS-WITH-NOTES. Applied 2 clear fixes to Stack table: TypeScript 6.x annotated as deliberate hold-back vs TS7 (GA'd 10 days before this spine); shadcn CLI flag annotated to verify exact spelling at use time since Base UI is now the CLI default. better-sqlite3/Express5/jose assumptions all confirmed correct. Full review: reviews/review-versions.md
- (event) Reviewer Gate, adversarial-divergence lens: FAIL. 5 findings, all fixable without new coaching: (1) CRITICAL - AD-4 same-file collision (two Frontend stories could both author useTaskBoard.ts) plus no pinned hook signature; (2) HIGH - AD-11 doesn't canonicalize empty description (null vs '' vs omitted) or pin request-body shape; (3) HIGH - no canonical Auth/User DTO, the '...' in {data:{token,...}} is unspecified; (4) HIGH - AD-5 gap-exhaustion undefined and bans reindex with no replacement; (5) MEDIUM - AD-8 doesn't pin status-code taxonomy or error-string shape, AD-7 doesn't pin req.user vs req.userId. Full review: reviews/review-divergence.md. Applying all 5 as direct spine fixes now (no new user-facing coaching needed - these are mechanical tightenings, not product decisions).
- (event) Divergence-lens findings 1-5 all closed by direct spine edits: AD-4 gained a pinned useTaskBoard signature + single-authoring-subunit rule for shared same-role files; AD-5 gained a bounded per-column reindex exception for gap exhaustion + server-authoritative sort; AD-7 gained req.userId convention; AD-8 gained status-code taxonomy + plain-string error rule; AD-11 gained description-null canonicalization + request-body shapes; new AD-13 added for canonical Auth/User response DTO. Re-ran lint_spine.py: same 5 false-positive placeholder findings as before (already dismissed), no new issues, no AD structural errors. No open assumptions or questions remain - proceeding to Close.
- (event) spine finalized
- (decision by user) Update, while starting Story 1.1 dispatch: fixed 2 issues that surfaced during real wave classification. (1) _bmad_output (underscore, typo) corrected to the real path _bmad-output/implementation-artifacts/ everywhere in the spine (paradigm text, AD-1, Structural Seed). (2) api.ts ownership moved from Integrator to Frontend Dev, closing a real sequencing conflict: Frontend's Wave 4 needs to call api.ts functions that didn't exist yet since Integrator was Wave 5. AD-1 and AD-10 updated accordingly; Integrator now owns only tests/ + .env. No AD Rule/Prevents substance changed beyond this ownership correction - not re-running the full Reviewer Gate for a non-structural fix, but flagging here for audit.
